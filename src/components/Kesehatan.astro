---
// Kesehatan.astro
---

<section class="section section-sehat" id="sehat" aria-labelledby="sehat-title">
  <div class="container">

    <header class="section-header">
      <span class="eyebrow reveal">🏥 Desa Sehat</span>
      <h2 class="section-title reveal" id="sehat-title">
        Data <span>Kesehatan</span> Masyarakat
      </h2>
      <p class="section-desc reveal">
        Hasil survei dan edukasi warga 1-on-1 oleh Tim KKNT 103 Kelompok 24 Universitas Sriwijaya untuk masyarakat Kelurahan Dempo Makmur. Data dapat digunakan untuk membantu meningkatkan kesehatan warga.
      </p>
    </header>

    <!-- ===== LIVE DATA SURVEI LAPANGAN ===== -->
    <div class="live-survey-wrap reveal">
      <div class="live-survey-header">
        <div class="live-survey-title">
          <span class="live-dot" aria-hidden="true"></span>
          <strong>Data Survei Real-Time</strong>
          <span class="live-survey-sub">Door-to-Door & Posyandu · KKNT 103 Kelompok 24</span>
        </div>
        <span class="live-survey-status" id="kes-api-status">⏳ Menghubungkan…</span>
      </div>
      <div class="live-survey-grid" id="kes-stats-grid">
        <div class="kes-stat-skeleton"></div>
        <div class="kes-stat-skeleton"></div>
        <div class="kes-stat-skeleton"></div>
        <div class="kes-stat-skeleton"></div>
        <div class="kes-stat-skeleton"></div>
        <div class="kes-stat-skeleton"></div>
      </div>
    </div>

    <!-- ===== PREVALENSI BALITA (NEW) ===== -->
    <div class="prevalensi-wrap reveal">
      <div class="sub-section-header">
        <div class="sub-section-text">
          <span class="sub-eyebrow">📊 Data Balita Per RW</span>
          <h3 class="sub-title">Prevalensi Karies &amp; Stunting</h3>
        </div>
        <p class="sub-note">Sumber: Pendataan Posyandu · KKNT 103 Kelompok 24</p>
      </div>
      <div id="prevalensi-grid" class="prevalensi-grid">
        <div class="prev-skeleton"></div>
        <div class="prev-skeleton"></div>
        <div class="prev-skeleton"></div>
      </div>
    </div>

    <!-- ===== GRID KARTU KESEHATAN ===== -->
    <div class="health-grid">

      <!-- Card 1: Kesehatan Gigi & Mulut -->
      <article
        class="health-card reveal reveal-delay-1"
        id="card-gigi"
        style="--card-accent: linear-gradient(90deg, #059669, #34d399);"
      >
        <div class="health-card-icon" style="--icon-bg: #d1fae5;" aria-hidden="true">🦷</div>
        <p class="health-card-eyebrow">Kesehatan Gigi &amp; Mulut</p>
        <h3 class="health-card-title">Kebiasaan Sikat Gigi Warga</h3>

        <div class="pictogram-row" role="img" id="gigi-pic-row" aria-label="Kebiasaan sikat gigi warga">
          <!-- Dimuat dinamis via JS -->
          <div class="kes-shimmer-text" style="width: 100%; height: 1.5rem;"></div>
        </div>

        <p class="pictogram-label" id="gigi-pic-label">
          <span class="kes-shimmer-text kes-shimmer-long"></span>
        </p>

        <div class="progress-wrap">
          <div class="progress-label">
            <span>Rutin Sikat Gigi</span><span id="gigi-pct-text"><span class="kes-shimmer-text"></span></span>
          </div>
          <div class="rw-progress-bar">
            <div class="rw-progress-fill" id="gigi-progress-fill" data-width="0%" style="width: 0%;"></div>
          </div>
        </div>
      </article>

      <!-- Card 2: Sanitasi -->
      <article
        class="health-card reveal reveal-delay-2"
        id="card-sanitasi"
        style="--card-accent: linear-gradient(90deg, #0284c7, #38bdf8);"
      >
        <div class="health-card-icon" style="--icon-bg: #e0f2fe;" aria-hidden="true">🚿</div>
        <p class="health-card-eyebrow">Sanitasi &amp; Lingkungan</p>
        <h3 class="health-card-title">Fasilitas Jamban &amp; Air</h3>

        <div class="pct-ring-wrap">
          <svg class="pct-ring" viewBox="0 0 90 90" role="img" aria-label="Persentase jamban sehat">
            <circle cx="45" cy="45" r="36" fill="none" stroke="#e0f2fe" stroke-width="9"/>
            <circle
              cx="45" cy="45" r="36" fill="none" stroke="#0284c7" stroke-width="9"
              stroke-dasharray="226.2" stroke-dashoffset="226.2"
              stroke-linecap="round"
              transform="rotate(-90 45 45)"
              class="ring-animate" data-offset="226.2"
              id="sanitasi-ring-fill"
            />
          </svg>
          <div class="pct-ring-text">
            <div class="pct-ring-value" style="color:#0284c7;" id="sanitasi-pct-val"><span class="kes-shimmer-text"></span></div>
            <div class="pct-ring-desc" id="sanitasi-pct-desc">Rumah tangga memiliki jamban sehat.</div>
          </div>
        </div>

        <div class="pictogram-row" id="sanitasi-pic-row" role="img" aria-label="Pictogram sanitasi" style="margin-top:0.75rem;">
          <div class="kes-shimmer-text" style="width: 100%; height: 1.5rem;"></div>
        </div>
      </article>

      <!-- Card 3: Sampah -->
      <article
        class="health-card reveal reveal-delay-2"
        id="card-sampah"
        style="--card-accent: linear-gradient(90deg, #ec4899, #f472b6);"
      >
        <div class="health-card-icon" style="--icon-bg: #fce7f3;" aria-hidden="true">🗑️</div>
        <p class="health-card-eyebrow">Sistem Pengelolaan Sampah</p>
        <h3 class="health-card-title">Pengelolaan Sampah Domestik</h3>

        <div class="pct-ring-wrap">
          <svg class="pct-ring" viewBox="0 0 90 90" role="img" aria-label="Persentase pengelolaan sampah">
            <circle cx="45" cy="45" r="36" fill="none" stroke="#fce7f3" stroke-width="9"/>
            <circle
              cx="45" cy="45" r="36" fill="none" stroke="#ec4899" stroke-width="9"
              stroke-dasharray="226.2" stroke-dashoffset="226.2"
              stroke-linecap="round"
              transform="rotate(-90 45 45)"
              class="ring-animate" data-offset="226.2"
              id="sampah-ring-fill"
            />
          </svg>
          <div class="pct-ring-text">
            <div class="pct-ring-value" style="color:#ec4899;" id="sampah-pct-val"><span class="kes-shimmer-text"></span></div>
            <div class="pct-ring-desc" id="sampah-pct-desc">Masyarakat sudah mampu memilah dan mengelola sampah.</div>
          </div>
        </div>

        <div class="pictogram-row" id="sampah-pic-row" role="img" aria-label="Pictogram sampah" style="margin-top:0.75rem;">
          <div class="kes-shimmer-text" style="width: 100%; height: 1.5rem;"></div>
        </div>
      </article>

      <!-- Card 4: Tindakan Medis (Flowchart) -->
      <article
        class="health-card reveal reveal-delay-3"
        id="card-medis"
        style="--card-accent: linear-gradient(90deg, #f59e0b, #fbbf24);"
      >
        <div class="health-card-icon" style="--icon-bg: #fef3c7;" aria-hidden="true">🏥</div>
        <p class="health-card-eyebrow">Tindakan Medis</p>
        <h3 class="health-card-title">Ke Mana Warga Saat Sakit?</h3>

        <div class="flow-chart" role="img" aria-label="Alur tindakan medis warga">
          <div class="flow-start">😷 Warga Merasa Sakit</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-branches">
            <div class="flow-node flow-node-primary">
              <span class="node-icon">🏥</span>
              Ke Puskesmas / Klinik
              <span class="flow-pct" id="medis-pct-puskesmas"><span class="kes-shimmer-text" style="width: 40px;"></span></span>
            </div>
            <div class="flow-node flow-node-secondary">
              <span class="node-icon">💊</span>
              Obat Mandiri / Warung
              <span class="flow-pct" id="medis-pct-warung"><span class="kes-shimmer-text" style="width: 40px;"></span></span>
            </div>
          </div>
          <div class="flow-note">
            ✅ <strong>Target:</strong> Tingkatkan angka kunjungan medis ke fasilitas kesehatan yang layak.
          </div>
        </div>
      </article>

      <!-- Card 5: Posyandu / KIA -->
      <article
        class="health-card reveal reveal-delay-4"
        id="card-posyandu"
        style="--card-accent: linear-gradient(90deg, #8b5cf6, #a78bfa);"
      >
        <div class="health-card-icon" style="--icon-bg: #ede9fe;" aria-hidden="true">👶</div>
        <p class="health-card-eyebrow">Posyandu &amp; KIA</p>
        <h3 class="health-card-title">Kesehatan Ibu dan Anak (KIA)</h3>

        <div class="kia-highlight">
          <div class="kia-big-num" aria-hidden="true" id="kia-pct-val"><span class="kes-shimmer-text" style="width: 60px; height: 2rem;"></span></div>
          <div class="kia-text" id="kia-text-desc">
            <span class="kes-shimmer-text kes-shimmer-long"></span>
          </div>
        </div>

        <div class="kia-children-row" id="kia-children-row" role="img" aria-label="Tingkat tumbuh kembang anak">
          <div class="kes-shimmer-text" style="width: 100%; height: 1.5rem;"></div>
        </div>

        <p class="kia-badge" id="kia-stunting-badge">
          🛡️ <span class="kes-shimmer-text kes-shimmer-long" style="width: 180px;"></span>
        </p>
      </article>

    </div>
  </div>
</section>

<!-- Wave Divider -->
<div class="wave-divider" aria-hidden="true" style="background:white;">
  <svg viewBox="0 0 1440 60" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
    <path d="M0,30 C360,0 720,60 1080,30 C1260,15 1380,45 1440,30 L1440,60 L0,60 Z" fill="#f8fafc"/>
  </svg>
</div>

<!-- Lightbox -->
<div id="kes-lightbox" class="kes-lightbox" role="dialog" aria-modal="true" aria-label="Pratinjau foto" hidden>
  <div class="kes-lb-backdrop" id="kes-lb-backdrop"></div>
  <div class="kes-lb-container">
    <button class="kes-lb-close" id="kes-lb-close" aria-label="Tutup">✕</button>
    <img id="kes-lb-img" src="" alt="" class="kes-lb-img" />
    <div class="kes-lb-cap" id="kes-lb-cap"></div>
  </div>
</div>

<style>
  :root {
    --clr-gray-400: #9ca3af;
    --clr-gray-500: #6b7280;
    --clr-gray-700: #374151;
    --clr-gray-800: #1f2937;
  }

  .section-sehat {
    background: #f8fafc;
    padding: 4rem 0;
  }

  /* ===== Sub-section Shared Header ===== */
  .sub-section-header {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }
  .sub-eyebrow {
    display: block;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #059669;
    margin-bottom: 0.3rem;
  }
  .sub-title {
    font-size: 1.35rem;
    font-weight: 800;
    color: var(--clr-gray-800);
    margin: 0;
  }
  .sub-note {
    font-size: 0.75rem;
    color: var(--clr-gray-500);
    margin: 0;
    align-self: flex-end;
  }

  /* ===== Live Survey Stats Widget ===== */
  .live-survey-wrap {
    background: #fff;
    border-radius: 1.25rem;
    border: 1px solid rgba(16,185,129,.2);
    box-shadow: 0 10px 25px -5px rgba(16,185,129,.1);
    overflow: hidden;
    margin-bottom: 2.5rem;
  }
  .live-survey-header {
    display: flex; align-items: center; justify-content: space-between;
    padding: 1rem 1.5rem;
    background: linear-gradient(135deg, #f0fdf4, #ecfdf5);
    border-bottom: 1px solid rgba(16,185,129,.15);
    flex-wrap: wrap; gap: 0.5rem;
  }
  .live-survey-title {
    display: flex; align-items: center; gap: 0.75rem;
    font-size: 0.95rem; color: #831843;
  }
  .live-survey-sub { font-size: 0.75rem; color: #be185d; font-weight: 500; opacity: 0.8; }
  .live-dot {
    width: 10px; height: 10px; background: #ec4899; border-radius: 50%;
    box-shadow: 0 0 0 4px rgba(236,72,153,.25);
    animation: livePulse 2s ease-in-out infinite;
    flex-shrink: 0;
  }
  @keyframes livePulse {
    0%, 100% { box-shadow: 0 0 0 4px rgba(236,72,153,.25); }
    50%       { box-shadow: 0 0 0 8px rgba(236,72,153,.1); }
  }
  .live-survey-status {
    font-size: 0.75rem; font-weight: 700; color: #9d174d;
    background: #fce7f3; padding: 0.3rem 0.8rem;
    border-radius: 99px; border: 1px solid #fbcfe8;
    white-space: nowrap;
  }
  .live-survey-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
  }

  /* JS-injected stat card elements */
  :global(.kes-stat-card) {
    display: flex; flex-direction: column; align-items: center; justify-content: center;
    padding: 1.5rem 1rem; border-right: 1px solid #f1f5f9;
    text-align: center; transition: background 0.3s ease;
  }
  :global(.kes-stat-card:last-child) { border-right: none; }
  :global(.kes-stat-card:hover) { background: #fff1f2; }
  :global(.kes-stat-icon) { font-size: 1.75rem; margin-bottom: 0.5rem; }
  :global(.kes-stat-val) { font-size: 1.75rem; font-weight: 900; line-height: 1.1; margin-bottom: 0.25rem; }
  :global(.kes-stat-lbl) { font-size: 0.68rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.04em; color: var(--clr-gray-400); margin-bottom: 0.5rem; }
  :global(.kes-stat-badge) { font-size: 0.65rem; font-weight: 700; padding: 0.25rem 0.6rem; border-radius: 99px; }
  :global(.ks-green)  { background: #fce7f3; color: #9d174d; }
  :global(.ks-red)    { background: #fee2e2; color: #991b1b; }
  :global(.ks-yellow) { background: #fef3c7; color: #92400e; }
  :global(.ks-blue)   { background: #dbeafe; color: #1e40af; }
  :global(.ks-purple) { background: #ede9fe; color: #5b21b6; }

  /* Skeletons */
  .kes-stat-skeleton {
    height: 120px;
    background: linear-gradient(90deg, #f1f5f9 25%, #e2e8f0 50%, #f1f5f9 75%);
    background-size: 200% 100%;
    animation: skeletonShimmer 1.5s infinite;
    border-right: 1px solid #fff;
  }
  @keyframes skeletonShimmer {
    0%   { background-position: 200% 0; }
    100% { background-position: -200% 0; }
  }

  /* ===== Prevalensi Section ===== */
  .prevalensi-wrap {
    background: #fff;
    border-radius: 1.25rem;
    border: 1px solid #e2e8f0;
    box-shadow: 0 4px 20px -5px rgba(0,0,0,0.06);
    padding: 1.75rem;
    margin-bottom: 2.5rem;
  }
  .prevalensi-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1rem;
  }
  .prev-skeleton {
    height: 160px;
    border-radius: 0.75rem;
    background: linear-gradient(90deg, #f1f5f9 25%, #e2e8f0 50%, #f1f5f9 75%);
    background-size: 200% 100%;
    animation: skeletonShimmer 1.5s infinite;
  }

  /* JS-injected prevalensi card elements */
  :global(.prev-card) {
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    border-radius: 0.875rem;
    padding: 1.1rem 1.25rem;
    transition: box-shadow 0.3s ease, transform 0.3s ease;
  }
  :global(.prev-card:hover) {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px -5px rgba(0,0,0,0.08);
  }
  :global(.prev-card-header) {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1rem;
  }
  :global(.prev-rw) {
    font-size: 0.95rem;
    font-weight: 800;
    color: #1e3a5f;
    background: linear-gradient(135deg, #dbeafe, #e0f2fe);
    padding: 0.3rem 0.75rem;
    border-radius: 99px;
    border: 1px solid #bfdbfe;
  }
  :global(.prev-total) {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--clr-gray-500);
  }
  :global(.prev-bar-wrap) { margin-bottom: 0.65rem; }
  :global(.prev-bar-label) {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.75rem;
    color: var(--clr-gray-500);
    margin-bottom: 0.3rem;
  }
  :global(.prev-bar-label strong) { font-size: 0.85rem; }
  :global(.prev-bar-track) {
    height: 8px;
    background: #f1f5f9;
    border-radius: 99px;
    overflow: hidden;
  }
  :global(.prev-bar-fill) {
    height: 100%;
    border-radius: 99px;
    transition: width 1.2s cubic-bezier(0.25, 1, 0.5, 1);
  }
  :global(.prev-bar-karies)  { background: linear-gradient(90deg, #dc2626, #f87171); }
  :global(.prev-bar-stunting) { background: linear-gradient(90deg, #f97316, #fb923c); }
  :global(.prev-card-footer) {
    font-size: 0.7rem;
    color: var(--clr-gray-400);
    margin-top: 0.5rem;
    padding-top: 0.5rem;
    border-top: 1px solid #f1f5f9;
  }

  /* Shimmer Loading placeholders */
  .kes-shimmer-text {
    display: inline-block;
    height: 1rem;
    width: 50px;
    background: linear-gradient(90deg, #f1f5f9 25%, #e2e8f0 50%, #f1f5f9 75%);
    background-size: 200% 100%;
    animation: skeletonShimmer 1.5s infinite;
    border-radius: 4px;
    vertical-align: middle;
  }
  .kes-shimmer-long {
    width: 150px;
    height: 0.85rem;
  }

  /* ===== Health Cards Grid ===== */
  .health-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2.5rem;
  }
  .health-card {
    background: #ffffff;
    border-radius: 1.25rem;
    padding: 1.75rem;
    position: relative;
    border: 1px solid #e2e8f0;
    box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    display: flex; flex-direction: column;
  }
  .health-card::before {
    content: ''; position: absolute;
    top: 0; left: 0; right: 0; height: 5px;
    background: var(--card-accent);
    border-radius: 1.25rem 1.25rem 0 0;
  }
  .health-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 25px -5px rgba(0,0,0,0.1);
  }
  .health-card-icon {
    width: 48px; height: 48px;
    background: var(--icon-bg);
    border-radius: 12px;
    display: grid; place-items: center;
    font-size: 1.5rem; margin-bottom: 1.25rem;
  }
  .health-card-eyebrow {
    font-size: 0.75rem; font-weight: 700; text-transform: uppercase;
    color: var(--clr-gray-400); letter-spacing: 0.05em; margin-bottom: 0.25rem;
  }
  .health-card-title {
    font-size: 1.25rem; font-weight: 800; color: var(--clr-gray-800);
    margin-bottom: 1.25rem; line-height: 1.3;
  }

  /* Pictograms */
  .pictogram-row { display: flex; flex-wrap: wrap; gap: 0.4rem; margin-bottom: 0.75rem; }
  .pictogram-item { font-size: 1.5rem; line-height: 1; transition: transform 0.2s; }
  .pictogram-item:hover { transform: scale(1.2); }
  .pictogram-label { font-size: 0.9rem; color: var(--clr-gray-700); line-height: 1.5; margin-bottom: 1rem; }

  /* Ring Charts */
  .pct-ring-wrap { display: flex; align-items: center; gap: 1.25rem; margin-bottom: 1rem; }
  .pct-ring { width: 80px; height: 80px; flex-shrink: 0; }
  .ring-animate { transition: stroke-dashoffset 2s cubic-bezier(0.25, 1, 0.5, 1); }
  .pct-ring-text { flex: 1; }
  .pct-ring-value { font-size: 2rem; font-weight: 900; line-height: 1; margin-bottom: 0.25rem; }
  .pct-ring-desc { font-size: 0.85rem; color: var(--clr-gray-500); line-height: 1.4; }

  /* Flow Chart */
  .flow-chart { background: #f8fafc; border-radius: 12px; padding: 1.25rem; text-align: center; }
  .flow-start { font-weight: 700; color: var(--clr-gray-800); font-size: 0.95rem; }
  .flow-arrow { font-size: 1.25rem; color: var(--clr-gray-400); margin: 0.5rem 0; }
  .flow-branches { display: flex; gap: 1rem; justify-content: center; margin-bottom: 1rem; }
  .flow-node { flex: 1; padding: 0.75rem; border-radius: 8px; font-size: 0.8rem; font-weight: 600; display: flex; flex-direction: column; align-items: center; gap: 0.25rem; }
  .flow-node-primary { background: #ecfccb; border: 1px solid #bef264; color: #3f6212; }
  .flow-node-secondary { background: #fee2e2; border: 1px solid #fca5a5; color: #991b1b; }
  .node-icon { font-size: 1.25rem; }
  .flow-pct { font-size: 1.25rem; font-weight: 900; margin-top: 0.25rem; }
  .flow-note { font-size: 0.75rem; color: #047857; background: #d1fae5; padding: 0.5rem; border-radius: 6px; }

  /* KIA / Posyandu */
  .kia-highlight { display: flex; align-items: center; gap: 1rem; background: #f5f3ff; padding: 1rem; border-radius: 12px; border: 1px solid #ddd6fe; margin-bottom: 1.25rem; }
  .kia-big-num { font-size: 2.5rem; font-weight: 900; color: #7c3aed; line-height: 1; }
  .kia-text strong { display: block; font-size: 0.95rem; color: #5b21b6; margin-bottom: 0.25rem; }
  .kia-text span { font-size: 0.8rem; color: #7c3aed; line-height: 1.4; display: block; }
  .kia-children-row { display: flex; gap: 0.5rem; justify-content: center; font-size: 1.75rem; margin-bottom: 1rem; }
  .kia-badge { font-size: 0.85rem; color: #047857; background: #d1fae5; padding: 0.5rem 1rem; border-radius: 99px; display: inline-block; font-weight: 600; margin-bottom: 1.25rem; }

  /* Progress Bars */
  .progress-wrap { margin-top: auto; padding-top: 1rem; }
  .progress-label { display: flex; justify-content: space-between; font-size: 0.8rem; font-weight: 600; color: var(--clr-gray-500); margin-bottom: 0.4rem; }
  .rw-progress-bar { width: 100%; height: 8px; background: #f1f5f9; border-radius: 99px; overflow: hidden; }
  .rw-progress-fill { height: 100%; background: var(--card-accent); border-radius: 99px; transition: width 1.5s cubic-bezier(0.25, 1, 0.5, 1); width: 0%; }

  /* ===== Galeri Section ===== */
  .galeri-wrap {
    background: #fff;
    border-radius: 1.25rem;
    border: 1px solid #e2e8f0;
    box-shadow: 0 4px 20px -5px rgba(0,0,0,0.06);
    padding: 1.75rem;
  }
  .galeri-tabs {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
  .gtab {
    font-size: 0.8rem;
    font-weight: 700;
    padding: 0.45rem 1rem;
    border-radius: 99px;
    border: 1.5px solid #e2e8f0;
    background: #f8fafc;
    color: var(--clr-gray-500);
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .gtab:hover { border-color: #ec4899; color: #9d174d; }
  .gtab.active {
    background: linear-gradient(135deg, #ec4899, #be185d);
    border-color: transparent;
    color: #fff;
    box-shadow: 0 4px 10px -2px rgba(236,72,153,0.4);
  }
  .galeri-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 1rem;
    margin-top: 1.25rem;
  }
  .galeri-skeleton {
    height: 280px;
    border-radius: 0.875rem;
    background: linear-gradient(90deg, #f1f5f9 25%, #e2e8f0 50%, #f1f5f9 75%);
    background-size: 200% 100%;
    animation: skeletonShimmer 1.5s infinite;
  }
  .galeri-empty {
    text-align: center;
    color: var(--clr-gray-400);
    padding: 3rem 0;
    font-size: 0.95rem;
    margin: 0;
  }

  /* JS-injected galeri card elements */
  :global(.galeri-card) {
    border-radius: 0.875rem;
    overflow: hidden;
    border: 1px solid #e2e8f0;
    background: #fff;
    cursor: pointer;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  :global(.galeri-card:hover) {
    transform: translateY(-4px);
    box-shadow: 0 12px 28px -5px rgba(0,0,0,0.12);
  }
  :global(.galeri-thumb) {
    position: relative;
    aspect-ratio: 4/3;
    overflow: hidden;
    background: #f1f5f9;
  }
  :global(.galeri-thumb img) {
    width: 100%; height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
  }
  :global(.galeri-card:hover .galeri-thumb img) { transform: scale(1.05); }
  :global(.galeri-img-err) {
    display: flex; align-items: center; justify-content: center;
    width: 100%; height: 100%;
    font-size: 2rem; color: var(--clr-gray-400);
    background: #f8fafc;
  }
  :global(.galeri-tipe-badge) {
    position: absolute;
    top: 0.6rem; left: 0.6rem;
    font-size: 0.65rem;
    font-weight: 700;
    padding: 0.25rem 0.6rem;
    border-radius: 99px;
    backdrop-filter: blur(8px);
  }
  :global(.badge-ds)   { background: rgba(16,185,129,0.85); color: #fff; }
  :global(.badge-toga) { background: rgba(34,197,94,0.85);  color: #fff; }
  :global(.galeri-info) { padding: 0.875rem; }
  :global(.galeri-kegiatan) {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--clr-gray-800);
    margin: 0 0 0.25rem;
    line-height: 1.3;
  }
  :global(.galeri-ket) {
    font-size: 0.78rem;
    color: var(--clr-gray-500);
    margin: 0 0 0.75rem;
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  :global(.galeri-meta) {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
  :global(.galeri-rw-badge) {
    font-size: 0.65rem;
    font-weight: 700;
    background: #dbeafe;
    color: #1e40af;
    padding: 0.2rem 0.5rem;
    border-radius: 99px;
  }
  :global(.galeri-date) {
    font-size: 0.68rem;
    color: var(--clr-gray-400);
  }

  /* ===== Lightbox ===== */
  .kes-lightbox {
    position: fixed;
    inset: 0;
    z-index: 9000;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
  }
  .kes-lightbox[hidden] { display: none; }
  .kes-lb-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,0.85);
    backdrop-filter: blur(6px);
  }
  .kes-lb-container {
    position: relative;
    z-index: 1;
    max-width: min(90vw, 800px);
    width: 100%;
    animation: lbFadeIn 0.25s ease;
  }
  @keyframes lbFadeIn {
    from { opacity: 0; transform: scale(0.95); }
    to   { opacity: 1; transform: scale(1); }
  }
  .kes-lb-close {
    position: absolute;
    top: -3rem;
    right: 0;
    width: 2.25rem; height: 2.25rem;
    background: rgba(255,255,255,0.15);
    border: 1px solid rgba(255,255,255,0.3);
    border-radius: 50%;
    color: #fff;
    font-size: 1rem;
    cursor: pointer;
    display: grid; place-items: center;
    transition: background 0.2s;
  }
  .kes-lb-close:hover { background: rgba(255,255,255,0.3); }
  .kes-lb-img {
    width: 100%;
    border-radius: 0.875rem;
    display: block;
    max-height: 70vh;
    object-fit: contain;
    background: #1a1a2e;
  }
  .kes-lb-cap {
    margin-top: 0.875rem;
    text-align: center;
    color: rgba(255,255,255,0.9);
    display: flex; flex-direction: column; gap: 0.25rem;
  }
  :global(.kes-lb-cap strong) { font-size: 1rem; font-weight: 700; }
  :global(.kes-lb-cap span)   { font-size: 0.85rem; opacity: 0.75; }
  :global(.kes-lb-cap .lb-rw) { font-size: 0.75rem; opacity: 0.6; }

  /* ===== Responsive ===== */
  @media (max-width: 768px) {
    .live-survey-grid { grid-template-columns: repeat(2, 1fr); }
    :global(.kes-stat-card) { border-bottom: 1px solid #f1f5f9; }
    :global(.kes-stat-card:nth-child(even)) { border-right: none; }
    .health-grid { grid-template-columns: 1fr; }
    .galeri-grid { grid-template-columns: repeat(2, 1fr); }
    .sub-section-header { flex-direction: column; align-items: flex-start; }
  }
  @media (max-width: 480px) {
    .galeri-grid { grid-template-columns: 1fr; }
  }
</style>

<script>
/* ---------- Scroll Reveal & Animations ---------- */
document.addEventListener("DOMContentLoaded", () => {

  // Fade/Slide Reveal
  const revealEls = document.querySelectorAll('.reveal');
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        revealObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
  revealEls.forEach(el => revealObserver.observe(el));

  // Progress Bar Animation
  const progressFills = document.querySelectorAll('.rw-progress-fill');
  const progressObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const fill = entry.target as HTMLElement;
        const targetWidth = fill.getAttribute('data-width');
        if (targetWidth) setTimeout(() => { fill.style.width = targetWidth; }, 300);
        progressObserver.unobserve(fill);
      }
    });
  }, { threshold: 0.5 });
  progressFills.forEach(fill => progressObserver.observe(fill));

  // SVG Ring Animation
  const rings = document.querySelectorAll('.ring-animate');
  const ringObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const ring = entry.target as SVGElement;
        const targetOffset = ring.getAttribute('data-offset');
        if (targetOffset) setTimeout(() => { (ring as any).style.strokeDashoffset = targetOffset; }, 400);
        ringObserver.unobserve(ring);
      }
    });
  }, { threshold: 0.5 });
  rings.forEach(ring => ringObserver.observe(ring));

});

/* ---------- Live API Fetch (Unified) ---------- */
(function () {
  const API_URL = 'https://script.googleusercontent.com/macros/echo?user_content_key=AUkAhnQjZyOnFpb-cxOFHqzjoPq_m6f733zhOBiy5EW-5XySDs5NMUo4sSXUTgiDdUPyeP6doL9xmREJvuTJ4mD5AVpDqwIq94KT71syJchqgXv45vhYO_LJ3K55uW91vyV5Q5-Okzng24UyXDz-IZcc82WMgVq91t5EXTBepv1bjvlV2jg4Lc3iaTbTAzbbjXFB688ysePGnE8-ncAHUq8ZdqBV8sKiyMqDvFzMP05fIJXsaq9B_XpdTBNknCCMj1_DNoIXBrfO3vRaM7bA79XoW-8Ia08b3A&lib=MDLAdFgRGOJVJ01oTM4DbJHdi3ZE4dG0b';

  const LOCAL_IMAGES: Record<string, string> = {
    '1zgN7jYjf4TlX87decR1sNdKAifaDSqA9': '/images/galeri/posyandu_kader.jpg',
    '12X6-h_FEgGQx5P4IlCv2c2lLrQm5NsyR': '/images/galeri/posyandu_anak.jpg',
    '1KqHvoTxVBG1g8Zb0nocjCfzOMSvBZP3r': '/images/galeri/modul_toga.jpg',
    '1rnnOkHVtnBn6teA1aXELsdU9FIKANSSW': '/images/galeri/olahraga.jpg',
    '11-uWFl8J9eXZzAaQureuND2c1Ypqpg69': '/images/galeri/gotong_royong.jpg',
  };

  /* -- Helpers -- */
  function driveUrl(raw: string): string {
    if (!raw) return '';
    let fileId = '';
    const ucMatch = raw.match(/[?&]id=([^&]+)/);
    if (ucMatch) fileId = ucMatch[1];
    if (!fileId) {
      const fileMatch = raw.match(/\/d\/([^/?]+)/);
      if (fileMatch) fileId = fileMatch[1];
    }
    if (fileId) {
      // Menggunakan file lokal jika tersedia agar terhindar dari rate limit Google Drive
      if (LOCAL_IMAGES[fileId]) {
        return LOCAL_IMAGES[fileId];
      }
      return `https://lh3.googleusercontent.com/d/${fileId}`;
    }
    return raw;
  }

  function formatDate(iso: string): string {
    try {
      return new Date(iso).toLocaleDateString('id-ID', {
        day: 'numeric', month: 'long', year: 'numeric'
      });
    } catch { return iso; }
  }

  /* -- Render: Live Survey Stats -- */
  function renderKesStats(rawData: any[]): void {
    const grid   = document.getElementById('kes-stats-grid');
    const status = document.getElementById('kes-api-status');
    if (!grid) return;

    let totalKK = 0, totalJiwa = 0, jamban = 0, nyeri = 0, dokter = 0, rentan = 0;
    rawData.forEach((kk: any) => {
      totalKK++;
      totalJiwa += Number(kk.anggota_keluarga) || 0;
      if (kk.kondisi_jamban === 'Baik/Layak')                    jamban++;
      if (kk.keluhan_nyeri_gigi === 'Ya')                        nyeri++;
      if (kk.tindakan_medis_gigi === 'Pernah ke Dokter Gigi')    dokter++;
      if (kk.kelompok_rentan && kk.kelompok_rentan !== 'Tidak Ada') rentan++;
    });

    const pSanitasi = totalKK ? Math.round(jamban  / totalKK * 100) : 0;
    const pNyeri    = totalKK ? Math.round(nyeri   / totalKK * 100) : 0;
    const pDokter   = totalKK ? Math.round(dokter  / totalKK * 100) : 0;

    if (status) status.innerHTML = `🟢 Live · ${totalKK} KK Terdata`;

    grid.innerHTML = `
      <div class="kes-stat-card">
        <div class="kes-stat-icon">🏠</div>
        <div class="kes-stat-val" style="color:#059669">${totalKK}</div>
        <div class="kes-stat-lbl">KK Tersurvei</div>
        <span class="kes-stat-badge ks-green">Data Lapangan</span>
      </div>
      <div class="kes-stat-card">
        <div class="kes-stat-icon">👥</div>
        <div class="kes-stat-val" style="color:#1d4ed8">${totalJiwa}</div>
        <div class="kes-stat-lbl">Jiwa Terdata</div>
        <span class="kes-stat-badge ks-blue">Akumulatif</span>
      </div>
      <div class="kes-stat-card">
        <div class="kes-stat-icon">💧</div>
        <div class="kes-stat-val" style="color:${pSanitasi >= 90 ? '#059669' : '#f97316'}">${pSanitasi}%</div>
        <div class="kes-stat-lbl">Sanitasi Layak</div>
        <span class="kes-stat-badge ${pSanitasi >= 90 ? 'ks-green' : 'ks-yellow'}">${pSanitasi >= 90 ? '✅ Sangat Baik' : '⚠️ Perlu Perhatian'}</span>
      </div>
      <div class="kes-stat-card">
        <div class="kes-stat-icon">🦷</div>
        <div class="kes-stat-val" style="color:#dc2626">${pNyeri}%</div>
        <div class="kes-stat-lbl">Keluhan Nyeri Gigi</div>
        <span class="kes-stat-badge ks-red">1 Bulan Terakhir</span>
      </div>
      <div class="kes-stat-card">
        <div class="kes-stat-icon">🏥</div>
        <div class="kes-stat-val" style="color:${pDokter >= 30 ? '#059669' : '#dc2626'}">${pDokter}%</div>
        <div class="kes-stat-lbl">Akses ke Dokter</div>
        <span class="kes-stat-badge ${pDokter >= 30 ? 'ks-green' : 'ks-red'}">${pDokter >= 30 ? '✅ Cukup' : '🆘 Kritis'}</span>
      </div>
      <div class="kes-stat-card">
        <div class="kes-stat-icon">⚠️</div>
        <div class="kes-stat-val" style="color:#7c3aed">${rentan}</div>
        <div class="kes-stat-lbl">Kelompok Rentan</div>
        <span class="kes-stat-badge ks-purple">Balita & Lansia</span>
      </div>
    `;
  }

  /* -- Render: Prevalensi Balita per RW -- */
  function renderPrevalensi(data: any[]): void {
    const grid = document.getElementById('prevalensi-grid');
    if (!grid || !data.length) {
      if (grid) grid.innerHTML = '<p style="color:#9ca3af;font-size:0.85rem;grid-column:1/-1;">Data prevalensi belum tersedia.</p>';
      return;
    }

    const maxKaries  = Math.max(...data.map((d: any) => d.persen_karies  || 0));
    const maxStunting = Math.max(...data.map((d: any) => d.persen_stunting || 0));

    grid.innerHTML = data.map((d: any) => {
      const pKaries   = Math.round((d.persen_karies  || 0) * 100);
      const pStunting = Math.round((d.persen_stunting || 0) * 100);
      const barK = maxKaries   ? Math.round((d.persen_karies  || 0) / maxKaries   * 100) : 0;
      const barS = maxStunting ? Math.round((d.persen_stunting || 0) / maxStunting * 100) : 0;
      return `
        <div class="prev-card">
          <div class="prev-card-header">
            <span class="prev-rw">${d.rw}</span>
            <span class="prev-total">${d.total_balita} balita</span>
          </div>
          <div class="prev-bar-wrap">
            <div class="prev-bar-label">
              <span>🦷 Karies</span><strong style="color:#dc2626">${pKaries}%</strong>
            </div>
            <div class="prev-bar-track">
              <div class="prev-bar-fill prev-bar-karies" style="width:0%" data-w="${barK}%"></div>
            </div>
          </div>
          <div class="prev-bar-wrap">
            <div class="prev-bar-label">
              <span>📉 Stunting</span><strong style="color:#f97316">${pStunting}%</strong>
            </div>
            <div class="prev-bar-track">
              <div class="prev-bar-fill prev-bar-stunting" style="width:0%" data-w="${barS}%"></div>
            </div>
          </div>
          <div class="prev-card-footer">
            Karies: ${d.karies ?? 0} anak · Stunting: ${d.stunting ?? 0} anak
          </div>
        </div>
      `;
    }).join('');

    // Animate bars on intersection
    const bars = grid.querySelectorAll('.prev-bar-fill');
    const barObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const el = entry.target as HTMLElement;
          setTimeout(() => { el.style.width = el.dataset.w || '0%'; }, 400);
          barObserver.unobserve(el);
        }
      });
    }, { threshold: 0.3 });
    bars.forEach(b => barObserver.observe(b));
  }

  /* -- Render: Galeri Kegiatan -- */
  let allGaleri: any[] = [];
  let currentItems: any[] = [];

  function renderGaleri(data: any[]): void {
    allGaleri = data;
    renderGaleriFiltered('all');

    document.querySelectorAll('.gtab').forEach(btn => {
      btn.addEventListener('click', () => {
        document.querySelectorAll('.gtab').forEach(b => {
          b.classList.remove('active');
          b.setAttribute('aria-selected', 'false');
        });
        btn.classList.add('active');
        btn.setAttribute('aria-selected', 'true');
        renderGaleriFiltered((btn as HTMLElement).dataset.filter || 'all');
      });
    });
  }

  function renderGaleriFiltered(filter: string): void {
    const grid  = document.getElementById('galeri-grid');
    const empty = document.getElementById('galeri-empty') as HTMLElement | null;
    if (!grid) return;

    currentItems = filter === 'all' ? allGaleri : allGaleri.filter((g: any) => g.tipe === filter);

    if (!currentItems.length) {
      grid.innerHTML = '';
      if (empty) empty.hidden = false;
      return;
    }
    if (empty) empty.hidden = true;

    const rwPad = (rw: number) => rw < 10 ? '0' + rw : '' + rw;

    grid.innerHTML = currentItems.map((g: any, i: number) => `
      <div class="galeri-card" data-idx="${i}" role="button" tabindex="0" aria-label="Lihat foto: ${g.keterangan}">
        <div class="galeri-thumb">
          <img
            src="${driveUrl(g.url_foto)}"
            alt="${g.keterangan}"
            loading="lazy"
            onerror="this.parentElement.innerHTML='<div class=\\'galeri-img-err\\'>📷</div>'"
          />
          <span class="galeri-tipe-badge ${g.tipe === 'DS' ? 'badge-ds' : 'badge-toga'}">${g.tipe === 'DS' ? '🏥 Desa Sehat' : '🌿 TOGA'}</span>
        </div>
        <div class="galeri-info">
          <p class="galeri-kegiatan">${g.kegiatan || '-'}</p>
          <p class="galeri-ket">${g.keterangan || ''}</p>
          <div class="galeri-meta">
            <span class="galeri-rw-badge">RW ${rwPad(g.rw)}</span>
            <span class="galeri-date">${formatDate(g.tanggal)}</span>
          </div>
        </div>
      </div>
    `).join('');

    grid.querySelectorAll('.galeri-card').forEach(card => {
      const open = () => openLightbox(Number((card as HTMLElement).dataset.idx));
      card.addEventListener('click', open);
      card.addEventListener('keydown', (e: Event) => {
        if ((e as KeyboardEvent).key === 'Enter' || (e as KeyboardEvent).key === ' ') open();
      });
    });
  }

  /* -- Render: Update Detailed Health Cards -- */
  function updateDetailedHealthCards(survei: any[], prevalensi: any[]): void {
    if (!survei || !survei.length) return;

    let totalKK = 0;
    let sikatGigi2x = 0;
    let jambanLayak = 0;
    let sampahTps = 0;
    let medisDokter = 0;
    let medisObat = 0;

    survei.forEach((kk: any) => {
      totalKK++;
      if (kk.perilaku_sikat_gigi === '2x Sehari') {
        sikatGigi2x++;
      }
      if (kk.kondisi_jamban === 'Baik/Layak') {
        jambanLayak++;
      }
      if (kk.pengelolaan_sampah === 'Dibuang ke TPS') {
        sampahTps++;
      }
      if (kk.tindakan_medis_gigi === 'Pernah ke Dokter Gigi') {
        medisDokter++;
      } else if (kk.tindakan_medis_gigi === 'Membeli Obat Saja') {
        medisObat++;
      }
    });

    const pSikat = totalKK ? Math.round((sikatGigi2x / totalKK) * 100) : 80;
    const pSani  = totalKK ? Math.round((jambanLayak / totalKK) * 100) : 90;
    const pSampah = totalKK ? Math.round((sampahTps / totalKK) * 100) : 82;

    const totalMedisGigi = medisDokter + medisObat;
    const pMedisDokter = totalMedisGigi ? Math.round((medisDokter / totalMedisGigi) * 100) : 62;
    const pMedisObat   = totalMedisGigi ? Math.round((medisObat / totalMedisGigi) * 100) : 38;

    // --- Card 1: Gigi & Mulut ---
    const gigiPicRow = document.getElementById('gigi-pic-row');
    const gigiPicLabel = document.getElementById('gigi-pic-label');
    const gigiPctText = document.getElementById('gigi-pct-text');
    const gigiProgressFill = document.getElementById('gigi-progress-fill') as HTMLElement | null;

    const activeSmileyCount = Math.round((pSikat / 100) * 10);
    if (gigiPicRow) {
      gigiPicRow.setAttribute('aria-label', `${activeSmileyCount} dari 10 warga sudah sikat gigi 2 kali sehari`);
      gigiPicRow.innerHTML = Array.from({ length: 10 }).map((_, i) => {
        if (i < activeSmileyCount) {
          return '<span class="pictogram-item" title="Sikat gigi rutin">😁</span>';
        } else {
          return '<span class="pictogram-item" style="opacity:0.25;" title="Belum rutin">😶</span>';
        }
      }).join('');
    }
    if (gigiPicLabel) {
      gigiPicLabel.innerHTML = `<strong>${activeSmileyCount} dari 10</strong> warga sudah sikat gigi 2× sehari (pagi &amp; malam).`;
    }
    if (gigiPctText) {
      gigiPctText.textContent = `${pSikat}%`;
    }
    if (gigiProgressFill) {
      gigiProgressFill.setAttribute('data-width', `${pSikat}%`);
      gigiProgressFill.style.width = `${pSikat}%`;
    }

    // --- Card 2: Sanitasi ---
    const saniRingFill = document.getElementById('sanitasi-ring-fill') as any;
    const saniPctVal = document.getElementById('sanitasi-pct-val');
    const saniPicRow = document.getElementById('sanitasi-pic-row');

    if (saniRingFill) {
      const circ = 226.2;
      const offset = circ - (circ * pSani) / 100;
      saniRingFill.setAttribute('stroke-dashoffset', String(offset));
      saniRingFill.setAttribute('data-offset', String(offset));
    }
    if (saniPctVal) {
      saniPctVal.textContent = `${pSani}%`;
    }
    const activeHouseCount = Math.round((pSani / 100) * 10);
    if (saniPicRow) {
      saniPicRow.setAttribute('aria-label', `${activeHouseCount} dari 10 rumah memiliki jamban sehat`);
      saniPicRow.innerHTML = Array.from({ length: 10 }).map((_, i) => {
        if (i < activeHouseCount) {
          return '<span class="pictogram-item">🏠</span>';
        } else {
          return '<span class="pictogram-item" style="opacity:0.25;">🏚️</span>';
        }
      }).join('');
    }

    // --- Card 3: Sampah ---
    const sampahRingFill = document.getElementById('sampah-ring-fill') as any;
    const sampahPctVal = document.getElementById('sampah-pct-val');
    const sampahPicRow = document.getElementById('sampah-pic-row');

    if (sampahRingFill) {
      const circ = 226.2;
      const offset = circ - (circ * pSampah) / 100;
      sampahRingFill.setAttribute('stroke-dashoffset', String(offset));
      sampahRingFill.setAttribute('data-offset', String(offset));
    }
    if (sampahPctVal) {
      sampahPctVal.textContent = `${pSampah}%`;
    }
    const activeTrashCount = Math.round((pSampah / 100) * 10);
    if (sampahPicRow) {
      sampahPicRow.setAttribute('aria-label', `${activeTrashCount} dari 10 rumah mengelola sampah dengan benar`);
      sampahPicRow.innerHTML = Array.from({ length: 10 }).map((_, i) => {
        if (i < activeTrashCount) {
          return '<span class="pictogram-item">🗑️</span>';
        } else {
          return '<span class="pictogram-item" style="opacity:0.25;">🗑️</span>';
        }
      }).join('');
    }

    // --- Card 4: Tindakan Medis ---
    const medisPctPuskesmas = document.getElementById('medis-pct-puskesmas');
    const medisPctWarung = document.getElementById('medis-pct-warung');

    if (medisPctPuskesmas) {
      medisPctPuskesmas.textContent = `${pMedisDokter}%`;
    }
    if (medisPctWarung) {
      medisPctWarung.textContent = `${pMedisObat}%`;
    }

    // --- Card 5: Posyandu / KIA ---
    if (prevalensi && prevalensi.length) {
      let totalBalita = 0;
      let totalStunting = 0;
      prevalensi.forEach((d: any) => {
        totalBalita += Number(d.total_balita) || 0;
        totalStunting += Number(d.stunting) || 0;
      });

      const pBebasStunting = totalBalita ? Math.round(((totalBalita - totalStunting) / totalBalita) * 100) : 100;

      const kiaPctVal = document.getElementById('kia-pct-val');
      const kiaTextDesc = document.getElementById('kia-text-desc');
      const kiaChildrenRow = document.getElementById('kia-children-row');
      const kiaStuntingBadge = document.getElementById('kia-stunting-badge');

      if (kiaPctVal) {
        kiaPctVal.textContent = `${pBebasStunting}%`;
      }
      if (kiaTextDesc) {
        kiaTextDesc.innerHTML = `
          <strong>Balita Tumbuh Optimal</strong>
          <span>Sebanyak ${totalBalita - totalStunting} dari ${totalBalita} balita di seluruh RW tumbuh optimal &amp; bebas stunting.</span>
        `;
      }
      if (kiaChildrenRow) {
        kiaChildrenRow.setAttribute('aria-label', `Prevalensi balita tumbuh optimal ${pBebasStunting}%`);
        const optimalChildEmojis = Math.round((pBebasStunting / 100) * 8);
        const childEmojis = ['👶', '👧', '👦'];
        kiaChildrenRow.innerHTML = Array.from({ length: 8 }).map((_, i) => {
          const emoji = childEmojis[i % childEmojis.length];
          if (i < optimalChildEmojis) {
            return `<span class="pictogram-item">${emoji}</span>`;
          } else {
            return `<span class="pictogram-item" style="opacity:0.25;" title="Kondisi Kurang Optimal">${emoji}</span>`;
          }
        }).join('');
      }
      if (kiaStuntingBadge) {
        if (totalStunting === 0) {
          kiaStuntingBadge.innerHTML = `🛡️ <strong>Zero Stunting</strong> – Indikator kesehatan balita di kelurahan sangat optimal!`;
        } else {
          kiaStuntingBadge.innerHTML = `🛡️ <strong>Optimalisasi Tumbuh Kembang</strong> – Terus tingkatkan asupan nutrisi &amp; kunjungan Posyandu!`;
        }
      }
    }
  }

  /* -- Lightbox -- */
  function openLightbox(idx: number): void {
    const g   = currentItems[idx];
    if (!g) return;
    const lb  = document.getElementById('kes-lightbox');
    const img = document.getElementById('kes-lb-img') as HTMLImageElement | null;
    const cap = document.getElementById('kes-lb-cap');
    if (!lb || !img || !cap) return;

    img.src = driveUrl(g.url_foto);
    img.alt = g.keterangan || '';
    const rwPad = (rw: number) => rw < 10 ? '0' + rw : '' + rw;
    cap.innerHTML = `
      <strong>${g.kegiatan || ''}</strong>
      <span>${g.keterangan || ''}</span>
      <span class="lb-rw">RW ${rwPad(g.rw)} · ${formatDate(g.tanggal)}</span>
    `;
    lb.hidden = false;
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox(): void {
    const lb = document.getElementById('kes-lightbox');
    if (lb) lb.hidden = true;
    document.body.style.overflow = '';
  }

  document.getElementById('kes-lb-close')?.addEventListener('click', closeLightbox);
  document.getElementById('kes-lb-backdrop')?.addEventListener('click', closeLightbox);
  document.addEventListener('keydown', (e: KeyboardEvent) => { if (e.key === 'Escape') closeLightbox(); });

  /* -- Main Fetch -- */
  function loadKesehatanData(url: string, isFallback: boolean = false): void {
    const status = document.getElementById('kes-api-status');
    fetch(url)
      .then(r => {
        if (!r.ok) throw new Error(`HTTP ${r.status}`);
        return r.json();
      })
      .then((payload: any) => {
        const survei     = Array.isArray(payload) ? payload : (payload.survei     ?? []);
        const prevalensi = payload.prevalensi ?? [];
        const galeri     = payload.galeri     ?? [];

        renderKesStats(survei);
        renderPrevalensi(prevalensi);
        updateDetailedHealthCards(survei, prevalensi);
        renderGaleri(galeri);

        if (status) {
          status.innerHTML = isFallback ? '🟢 Live · Local Mode' : `🟢 Live · ${survei.length} KK Terdata`;
        }
      })
      .catch((err) => {
        console.warn(`Fetch gagal untuk ${url}:`, err);
        if (!isFallback) {
          if (status) status.innerHTML = '⏳ Menghubungkan ke Lokal...';
          // Silent fallback ke data local yang sudah kita siapkan
          loadKesehatanData('/data-kesehatan-fallback.json', true);
        } else {
          if (status) status.innerHTML = '🟡 Offline';
          const grid = document.getElementById('kes-stats-grid');
          if (grid) grid.innerHTML = '<p style="padding:1.5rem;color:#9ca3af;font-size:0.9rem;text-align:center;grid-column:1/-1;">Data tidak tersedia saat ini.</p>';
        }
      });
  }

  loadKesehatanData(API_URL);

})();
</script>